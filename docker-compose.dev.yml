services:
  nerpybot-migrations:
    build:
      context: .
      target: migrations

  nerpybot:
    build:
      context: .
      target: bot

  humanmusic-migrations:
    build:
      context: .
      target: migrations
    environment:
      ALEMBIC_CONFIG: alembic-humanmusic.ini

  humanmusic:
    build:
      context: .
      target: bot
